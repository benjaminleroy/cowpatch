# runtime configuration for cowplot

rcParams = dict(maxIter=20,
                min_size_px=10,
                eps=1e-2,

                save_verbose=True,
                show_verbose=True,

                num_attempts=2,

                base_height=3.71,
                base_aspect_ratio=1.618 # the golden ratio

                )
