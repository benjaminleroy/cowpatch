import numpy as np

# class textgrob:
#     def __init__(self,
#                  ...):
#     # how to relate to https://plotnine.readthedocs.io/en/stable/generated/plotnine.themes.element_text.html#plotnine.themes.element_text?



# def get_legend(gg):
#     """
#     capture just legend of ggplot object and center in new "ggplot"
#     """

def val_range(x):
    return (np.min(x), np.max(x))

# class ggMatrix:

#     def __init__(nrow=None,
#                  ncol=None,
#                  )
# so we're tracking indices?
# as well as relative widths / heights
# but if this gets really complicated - how does the relative heights get corrected?

# this naturally does some "level" tracking...
